{% extends 'base.html.twig' %}

{% block title %}Produit
{% endblock %}

{% block body %}
	<h1>Produit</h1>

	<table class="table table-hover">
		<thead></thead>
		<tbody>
			<tr>
				<th scope="col">ID</th>
				<td>{{ produit.id }}</td>
			</tr>
		</tbody>
	</thead>

	<tr>
		<th>Ref</th>
		<td>{{ produit.ref }}</td>
	</tr>
	<tr>
		<th>Designation</th>
		<td>{{ produit.designation }}</td>
	</tr>
	<tr>
		<th>Prix_unit</th>
		<td>{{ produit.prixUnit }}</td>
	</tr>
</tbody></table><a href="{{ path('produit_index') }}">back to list</a><a href="{{ path('produit_edit', {'id': produit.id}) }}">edit</a>{{ include('produit/_delete_form.html.twig') }}{% endblock %}
